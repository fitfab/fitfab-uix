import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";

import { Carousel } from "./Carousel";
import {data} from "../../mockdata/slides";

<Meta title="Carousel" component={Carousel} />

# Carousel

This component relies on modern web apis and therefore works only in modern browsers. And these are: 

- IntersectionObserver(JS)
- scrollBy(JS)
- Scroll Snap(CSS)

<Canvas>
  <Story
    name="Default"
    args={{
      children: data,
    }}
  >
    {(args) => <Carousel {...args}>{args.children.map((slide, index) => <div key={index}><img src={slide.src} alt={slide.alt} width={slide.width} /></div>)}</Carousel>}
  </Story>
</Canvas>

<ArgsTable story="Default" />
